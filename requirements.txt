# are.na-toolkit — Python dependencies
# ======================================
# For the active Python scripts (arena-uploader, pinterest-scraper).
# build.py uses stdlib only and needs nothing installed.
#
# Setup:
#   cd ~/Development/are.na-toolkit
#   /usr/local/opt/python@3.14/bin/python3.14 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

requests>=2.28          # HTTP — used by arena-uploader.py and pinterest-scraper.py
beautifulsoup4>=4.11    # HTML parsing — used by pinterest-scraper.py

# ── Not included: _assets/art-palette/palette-embedding/ ─────────────────────
# That folder contains a Google Arts & Culture reference demo (2018) using
# TensorFlow 1.3 + numpy + scikit-image + annoy. It is third-party research
# code, non-runnable without porting to TF2, and is not part of the active
# toolkit. See _assets/art-palette/README.md for details.
